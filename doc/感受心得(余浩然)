成绩管理系统开发心得与感悟
参与本次成绩管理系统的开发，我主要负责了除 UI 交互层外的核心模块设计与实现，包括数据模型层、数据库持久化层、业务逻辑层（查找模块、排课 / 选课模块）等核心部分。这段开发经历让我对 C++ 面向对象编程、数据库交互、系统设计思路有了更深刻的理解，也积累了不少实战经验和问题解决思路。
一、从 “功能实现” 到 “设计思维” 的转变
最初接手开发时，我的第一想法是 “快速实现功能”，比如先写死学生、教师的属性和方法，直接完成选课、排课的基础逻辑。但在实际开发中发现，缺乏顶层设计的代码很快就会陷入混乱 —— 比如学生和教师有大量重复的属性（姓名、年龄、性别），重复编写不仅冗余，后续修改也需要多处调整。
于是我重新梳理了数据模型，抽象出Person基类封装公共属性和方法，让Student和Teacher继承该基类，既符合面向对象的 “封装” 和 “继承” 思想，也让代码结构更清晰。同时，针对 “系统需要统一管理所有学生、教师、课程数据” 的需求，设计了System单例类，避免了全局变量的滥用，也保证了内存中数据的唯一性和一致性。这个过程让我深刻体会到：“好的设计比单纯的功能实现更重要”，前期多花时间梳理结构，后期能大幅降低维护成本。
二、智能指针与内存管理的实战体会
C++ 的内存管理一直是重点和难点，本次开发中我全程使用shared_ptr和weak_ptr管理对象生命周期，彻底避开了野指针、内存泄漏等问题。比如在System类中，用vector<shared_ptr<Teacher>>存储教师列表，既保证了对象的共享访问，又能自动释放资源；在Course类中，用weak_ptr关联学生和教师，避免了循环引用导致的内存泄漏。
印象最深的是最初在Course类中误用shared_ptr关联学生 / 教师，导致对象析构时无法释放，通过调试和查阅资料，才理解weak_ptr“弱引用” 的核心价值 —— 只关联不拥有，既可以访问对象，又不会影响对象的生命周期。这让我明白：工具的正确使用比 “会用” 更重要，理解底层原理才能真正发挥 C++ 的优势。
三、数据库持久化的踩坑与成长
数据库层是本次开发的核心难点之一，我负责的operationToDB类需要实现建表、插入、关联校验等功能，过程中踩了不少坑：

    约束校验的适配：最初设计性别约束时只考虑了中文，后续需要适配英文输入，我修改了建表 SQL 的 CHECK 约束，同时调整了初始化数据的性别值，保证数据和约束的一致性；
    重复数据的处理：最初插入学生 / 教师时未考虑重复 ID，导致多次插入报错，后来通过ON CONFLICT DO NOTHING解决了重复插入问题；
    关联操作的校验：排课 / 选课前必须校验学生 / 教师 / 课程是否存在，否则会插入无效关联数据，因此在insertUserCourseRelation方法中增加了前置查询校验，确保关联的对象都存在。

这些问题让我意识到，数据库操作不仅要实现 “能写”，更要保证 “写得对、写得安全”，数据的完整性和一致性比功能实现更关键。同时，通过pqxx库操作 PostgreSQL，也让我熟悉了 C++ 与数据库交互的基本流程，理解了 “事务” 的重要性 —— 建表、插入等操作都封装在事务中，确保要么全部成功，要么全部回滚。
四、模块化开发与协作的思考
本次开发采用 C++20 的模块特性，将不同功能拆分为独立模块（如gradesystem:student、gradesystem:database），每个模块职责单一，既降低了耦合度，也方便后续维护。比如数据库模块完全独立，UI 层只需调用operationToDB的方法即可完成数据操作，无需关心底层实现。
虽然我未负责 UI 层，但在与 UI 层衔接时，我也充分考虑了接口的易用性：比如将数据库连接参数作为方法参数，让 UI 层可以灵活传入；返回 bool 值标识操作结果，方便 UI 层给出清晰的提示。这让我体会到，开发不仅要考虑自己负责的模块，还要兼顾与其他模块的衔接，“接口友好” 是协作开发的关键。
五、总结与收获
本次开发让我从 “只会写简单代码” 的阶段，进阶到能独立设计、实现一个小型系统的程度，最大的收获有三点：

    设计能力的提升：学会用面向对象的思想梳理业务逻辑，用单例、继承等模式解决实际问题，不再局限于 “堆砌代码”；
    问题解决能力的增强：遇到内存泄漏、数据库报错等问题时，从最初的慌乱，到后来能通过调试、查资料、梳理逻辑定位问题，培养了 “拆解问题、逐步解决” 的思路；
    工程化思维的建立：理解了 “数据一致性”“代码可维护性”“接口设计” 等工程化概念，明白优秀的代码不仅要 “能跑”，还要易读、易改、易扩展。

当然，开发过程中也发现了自己的不足：比如对 C++20 模块的特性理解还不够深入，数据库优化（如索引）未做考虑，异常处理的粒度还可以更细。后续我会针对性地补充这些知识点，同时尝试重构现有代码，比如增加日志模块、优化数据库查询效率，让系统更完善。
总的来说，这次开发是一次宝贵的实战经历，不仅巩固了 C++ 基础知识，更让我理解了 “编程是解决问题的艺术”—— 既要关注技术细节，也要站在系统层面思考问题。未来我会继续保持这种 “边做边学、边反思边优化” 的态度，不断提升自己的开发能力。
