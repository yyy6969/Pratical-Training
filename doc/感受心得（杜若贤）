成绩管理系统 UI 类开发心得
在完成成绩管理系统 UI 类的开发与实现后，我对 C++ 面向对象编程、模块化设计以及实际项目开发中的工程化思维有了更深刻的理解，现将开发过程中的核心心得总结如下：
一、架构设计：单例模式与模块化的实践价值
本次 UI 类采用单例模式实现核心实例管理（singletonUI()方法），这一设计选择让我深刻体会到设计模式在实际开发中的必要性：

    避免了 UI 类多次实例化导致的资源冲突（如重复初始化数据库、多次加载基础数据），保证整个系统中只有一个 UI 交互入口，符合控制台应用 “单一交互终端” 的业务特性；
    单例模式的私有化构造 / 析构函数、禁用拷贝 / 移动语义，从语法层面杜绝了错误实例化的可能，这是 C++ 封装特性的典型落地，也让我理解到 “语法约束” 比单纯的代码规范更能保证程序健壮性。

同时，代码基于 C++20 模块系统（export module gradesystem:ui）拆分功能模块，将 UI 交互、数据操作、业务逻辑解耦，比如 UI 类仅负责用户输入输出和流程调度，实际的数据库操作、学生 / 课程管理交由System/Enrollment/operationToDB等类完成。这种模块化设计让代码结构清晰，后续修改某一功能（如调整选课逻辑）时，只需改动对应模块，无需修改 UI 核心逻辑，大幅提升了代码的可维护性。
二、用户交互：健壮性与易用性的平衡
UI 类的核心职责是承接用户操作，因此 “输入校验” 和 “友好反馈” 成为开发重点：

    针对整数、浮点数输入封装了getIntInput()/getDoubleInput()工具函数，通过循环 + 异常捕获确保输入合法性，避免了因用户误输入非数字字符导致程序崩溃的问题。这让我意识到，控制台应用的 “健壮性” 首先体现在对用户输入的容错处理上，哪怕是简单的整数输入，也需要考虑边界情况；
    所有操作都添加了明确的日志反馈（如 “[成功] 数据库表初始化完成！”“选课失败：XXX”），并在主菜单中提示默认测试数据，降低了用户的使用成本。这种 “以用户视角设计交互” 的思路，让我跳出了单纯的 “功能实现” 思维，开始关注程序的实际使用体验。

三、数据一致性：内存与数据库的双向同步
UI 类在addTeacher()/addStudent()/addCourse()等方法中，实现了 “数据库插入 + 内存列表同步” 的逻辑，这是本次开发的核心难点，也让我理解到 “数据一致性” 在系统开发中的重要性：

    初始化阶段（initSystem()）先清空内存列表，再从数据库加载 / 预置测试数据，避免了内存数据与数据库数据不一致的问题；
    添加教师 / 学生 / 课程时，先确保数据库插入成功，再同步到System类的内存列表中，既保证了数据的持久化存储，又让后续的排课、选课操作能直接读取内存数据，兼顾了 “数据可靠性” 和 “操作效率”；
    异常捕获机制（try-catch）覆盖了数据库操作的全流程，哪怕数据插入失败，也能给出明确的错误提示，而非让程序异常退出，这是工业级代码与入门级代码的核心区别 —— 不仅要 “能跑”，还要 “稳跑”。

四、工程化思维：从 “功能实现” 到 “系统设计”
本次开发让我从单纯的 “写代码实现功能”，转向了 “站在系统层面设计代码”：

    依赖管理：UI 类通过导入database/:system/:enrollment等模块，明确了各组件的依赖关系，避免了全局变量和混乱的头文件包含，符合 C++ 现代工程的开发规范；
    可扩展性：UI 类的核心方法（如teacherArrangeCourse()/studentEnrollCourse()）仅负责接收用户输入并调用对应业务类的方法，不直接实现排课 / 选课逻辑，为后续扩展功能（如批量选课、退课）预留了接口；
    测试友好性：initTestData()方法预置了基础测试数据，无需手动输入即可验证核心功能，大幅提升了开发和测试效率，这让我意识到 “测试驱动开发” 的前置准备（如预置测试数据）同样重要。

五、不足与改进方向
开发过程中也暴露了自身的一些不足：

    异常处理的粒度可进一步细化：目前的catch块仅捕获通用的std::exception，可针对数据库连接失败、ID 重复等场景定义自定义异常，让错误提示更精准；
    交互体验可优化：控制台输入仅支持纯文本，可添加 “确认操作” 步骤（如选课成功后提示 “是否确认选课？”），减少误操作；
    内存管理可强化：虽然使用了shared_ptr管理对象，但未对weak_ptr的过期检查做更细致的处理（如打印所有数据时，可检查指针是否过期，避免访问已销毁的对象）。
